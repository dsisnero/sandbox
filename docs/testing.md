# Testing

## Required Gates

- Format: `make format`
- Lint: `make lint`
- Spec: `make test`

## Parity Testing

Porting changes should include focused parity checks against `vendor/codex` behavior.
When drift is found, document whether it is a bug or an intentional deviation.
