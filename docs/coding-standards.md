# Coding Standards

- Preserve upstream semantics first; Crystal idioms are secondary unless behavior is unchanged.
- Keep methods small and explicit.
- Avoid introducing runtime dependencies unless parity needs them.
- Add tests for every behavior change.
